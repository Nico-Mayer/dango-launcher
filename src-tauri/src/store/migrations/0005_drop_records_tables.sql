-- Snippets and quicklinks moved to text files in ~/.config/dango, so they travel
-- in the user's dotfiles. The tables are retired; authored content now lives in
-- snippets.json and quicklinks.json.
DROP TABLE IF EXISTS snippets;
DROP TABLE IF EXISTS quicklinks;
